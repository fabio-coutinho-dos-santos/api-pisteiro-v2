import { ErrorHandlerInterceptor } from './error-handler.interceptor';

describe('ErrorHandlerInterceptor', () => {
  it('should be defined', () => {
    expect(new ErrorHandlerInterceptor()).toBeDefined();
  });
});
